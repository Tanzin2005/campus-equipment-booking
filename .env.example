# Used by Docker Compose's PostgreSQL override only.
# Generate a random value: python -c "import secrets; print(secrets.token_hex(24))"
# Use the generated hexadecimal value below, then save this file as .env.
POSTGRES_PASSWORD=
# Local Python runs read process environment variables, not this file automatically.
